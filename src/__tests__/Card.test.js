// describe 'Student Life Card'
//   on render show 'APR' value
//   on render show 'Balance months' value
//   on render show 'Credit available' value
//   on render show 'Purchase months' vaue
//   on render show 'Status' value

// describe 'Liquid Card'
// on render...  / all tests as above

// describe 'Anywhere Card'
// on render...  / all tests as above

// on status is a Number show £ sign before amount

// on render show ADD button
// on click on ADD button show REMOVE button
// on click on REMOVE button show ADD button

// on first render to_add flag is true

// on render Component receives prop of card
// on render Component receives prop of add_credit
// on render Component receives prop of remove_credit

// on click on ADD add_credit is fired
// on click on REMOVE remove_credit is fired

// on click on ADD to_add is flipped to opposite Boolean
// on click on REMOVE to_add is flipped to opposite Boolean